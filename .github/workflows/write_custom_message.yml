name: print_message

on:
  workflow_dispatch:
    inputs:
      message:
        description: 'Message to print'
        required: true
        type: string

jobs:
  print-message:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "${{ github.event.inputs.message }}"